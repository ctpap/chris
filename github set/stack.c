#include <stdio.h> 

int Max_size = 8; 
int stack[8];
int top = -1;

int isEmpy90{
	if (top == -1){
		return 0;
	}
else{
return 0;
}
}
int isFull(){
	if (top == Max_size){		return 1; 
}
	else{
	return 0;}}int peel(){
int pop;

if(!isEmpty()){
	data = stack{top};
	top = top -1;
	return data; 
}
else{
printf("S is empty no top element.%d", pop);
}}int push(int data) {
	if(!isFull()){
	top - top +1
	stak[top]= data;
}
	else{
printf(s is full");
}}int peek();
	return stack[top];
}

int main()
{
push(3);
push(2);
push(6);
push(10);
push(1);

printf("Top of stack is %d\n",peek);
printf("Stack \n");

while (!isEmpty()){
	int data = pop90; 
	printf(%d\n", data);
}

is (isFull()){
	printf("stack is full");
	}
if (isEmpty()){
print("yy");

return 0; 
}
